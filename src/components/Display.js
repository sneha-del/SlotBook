import React, { Component } from "react";
class Display extends React.Component {
  render() {
    return (
      <span>
        <button>v</button>
      </span>
    );
  }
}

export default Display;
